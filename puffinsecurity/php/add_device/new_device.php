<?php    session_start();    include "../db/db_manipulation.php";    if (isset($_GET['token'])) {        $token = $_GET['token'];        $username = $_SESSION['uname'];        if(token_matches($token, $username)){            extraDevice($username);                    }        else{            echo "Въведеният код е грешен!";        }    }    else{        echo "Възникна проблем! Моля, опитайте отново.";    }?>